<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath/>
    </parent>

    <groupId>com.spotify</groupId>
    <artifactId>recommender</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>spotify-recommender</name>
    <description>Spotify Recommendation REST Service</description>

    <properties>
        <java.version>21</java.version>
        <!-- Latest available on repo.spring.io/milestone; verified by decompiling actual jars -->
        <spring-ai.version>1.1.8</spring-ai.version>
    </properties>

    <repositories>
        <repository>
            <id>spring-milestone</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <!-- WebClient (reactive HTTP client used in a servlet app) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!-- Required for JdbcOAuth2AuthorizedClientService -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- Anthropic Java SDK — verified groupId/artifactId/version on Maven Central
             (com.anthropic:sdk does not exist; the real coordinates are anthropic-java) -->
        <dependency>
            <groupId>com.anthropic</groupId>
            <artifactId>anthropic-java</artifactId>
            <version>2.43.0</version>
        </dependency>
        <!-- anthropic-java pulls in anthropic-java-client-okhttp, which needs okhttp at
             runtime. The pre-existing test-scoped okhttp pin below (added for mockwebserver)
             otherwise wins dependency mediation by nearness without widening to the scope the
             transitive dependency needs — Maven never auto-widens an explicit declaration's
             scope, so it silently shadowed the runtime need (confirmed via
             `mvn dependency:tree -Dverbose`: "scope not updated to runtime"), causing a
             NoClassDefFoundError on okhttp3.OkHttpClient$Builder at app startup. Pinning here at
             compile scope (available at runtime) fixes that. -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version>
        </dependency>
        <!--
          Spring AI 1.1.x from repo.spring.io/milestone:
          GA 1.0.0 (Maven Central) only ships SSE transport.
          Streamable HTTP support requires 1.1.x. This is
          Spring's official pre-release repo — upgrade to
          Maven Central GA when 1.1.x is published there.
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>

        <!-- Spring AI OpenAI: provides OpenAiEmbeddingModel for text-embedding-3-small calls.
             Version managed by the Spring AI BOM already declared above (1.1.8).
             EmbeddingConfig controls bean creation — app boots without OPENAI_API_KEY,
             RAG degrades to top-tracks context when the key is absent. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-openai</artifactId>
        </dependency>

        <!-- Load environment variables from .env on startup.
             Supplements manual export — either works. Never commit the real .env file. -->
        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>4.0.0</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- MockWebServer for SpotifyApiService tests -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <version>4.12.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- dev profile is always active for mvn spring-boot:run.
                         SPRING_PROFILES_ACTIVE in .env cannot do this — spring-dotenv
                         runs after Spring Boot evaluates profiles, so the timing is too late. -->
                    <profiles>
                        <profile>dev</profile>
                    </profiles>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
